// Generated by Firetrack. Do not edit by hand.

import Foundation

public enum AnalyticsValue: Equatable, Sendable {
    case string(String)
    case int(Int)
    case double(Double)
    case bool(Bool)
}

public extension AnalyticsValue {
    /// The underlying value, boxed for Firebase's untyped parameter dictionary.
    var firebaseValue: Any {
        switch self {
        case let .string(value):
            value
        case let .int(value):
            value
        case let .double(value):
            value
        case let .bool(value):
            value
        }
    }
}

public enum AnalyticsScreen: String, CaseIterable, Sendable {
    case driveRecordScreen = "DriveRecordScreen"
}

public enum SourceValue: String, CaseIterable, Sendable {
    case app = "app"
    case widget = "widget"
}

public enum AnalyticsEvent: Equatable, Sendable {
    case appOpened
    /// A drive recording finished.
    case recordingCompleted(distanceM: Double, durationSec: Int?, isManual: Bool, source: SourceValue)

    public var name: String {
        switch self {
        case .appOpened:
            "app_opened"
        case .recordingCompleted:
            "recording_completed"
        }
    }

    public var parameters: [String: AnalyticsValue] {
        switch self {
        case .appOpened:
            return [:]
        case let .recordingCompleted(distanceM, durationSec, isManual, source):
            var parameters: [String: AnalyticsValue] = [:]
            parameters["distance_m"] = .double(distanceM)
            if let durationSec {
                parameters["duration_sec"] = .int(durationSec)
            }
            parameters["is_manual"] = .bool(isManual)
            parameters["source"] = .string(source.rawValue)
            return parameters
        }
    }
}

public extension AnalyticsEvent {
    /// The event's parameters mapped into Firebase's untyped dictionary.
    var firebaseParameters: [String: Any] {
        parameters.mapValues(\.firebaseValue)
    }
}
